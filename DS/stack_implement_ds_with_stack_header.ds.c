#include <bits/stdc++.h>
#include "my_stack.h";
using namespace std;

int main()
{
    Stack st;
        st.pop();
    st.push(1);
    st.pop();

    st.push(2);
     st.push(3);
     st.push(4);
    st.pop();
     st.push(5);
      st.pop();




    return 0;
}
